package com.sorion.repository;

import com.sorion.domain.TrainingSessionAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrainingSessionAnswerRepository extends JpaRepository<TrainingSessionAnswer, Long> {
}
